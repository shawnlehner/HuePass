---
---

<footer class="footer">
  <div class="footer-container container">
    <div class="footer-content">
      <p class="footer-tagline">
        Free color contrast checking for accessible design
      </p>
      <p class="footer-attribution">
        Crafted with <span class="heart" aria-label="love">&#9829;</span> by
        <a href="https://www.shawnlehner.com/" target="_blank" rel="noopener noreferrer" class="author-link">
          Shawn Lehner
        </a>
      </p>
    </div>
    <div class="footer-meta">
      <p class="footer-opensource">
        <a href="https://github.com/shawnlehner/HuePass" target="_blank" rel="noopener noreferrer" class="opensource-link">Open source</a> and free to use.
      </p>
    </div>
  </div>
</footer>

<style>
  .footer {
    margin-top: auto;
    padding: var(--space-12) 0 var(--space-8);
    border-top: 1px solid var(--color-border-subtle);
    background: var(--color-bg-secondary);
  }

  .footer-container {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: var(--space-6);
    text-align: center;
  }

  .footer-content {
    display: flex;
    flex-direction: column;
    gap: var(--space-3);
  }

  .footer-tagline {
    font-size: var(--text-sm);
    color: var(--color-text-tertiary);
  }

  .footer-attribution {
    font-size: var(--text-base);
    color: var(--color-text-secondary);
  }

  .heart {
    display: inline-block;
    color: #ef4444;
    font-style: normal;
    animation: heartbeat 1.5s ease-in-out infinite;
  }

  @keyframes heartbeat {
    0%, 100% { transform: scale(1); }
    50% { transform: scale(1.1); }
  }

  .author-link {
    color: var(--color-accent);
    font-weight: 500;
    text-decoration: none;
    position: relative;
  }

  .author-link::after {
    content: '';
    position: absolute;
    bottom: -2px;
    left: 0;
    width: 100%;
    height: 1px;
    background: var(--color-accent);
    transform: scaleX(0);
    transform-origin: right;
    transition: transform var(--transition-base);
  }

  .author-link:hover::after {
    transform: scaleX(1);
    transform-origin: left;
  }

  .author-link:hover {
    color: var(--color-accent-hover);
  }

  .footer-meta {
    padding-top: var(--space-4);
    border-top: 1px solid var(--color-border-subtle);
    width: 100%;
  }

  .footer-opensource {
    font-size: var(--text-xs);
    color: var(--color-text-muted);
  }

  .opensource-link {
    color: var(--color-text-tertiary);
    text-decoration: none;
    transition: color var(--transition-fast);
  }

  .opensource-link:hover {
    color: var(--color-accent);
  }
</style>
